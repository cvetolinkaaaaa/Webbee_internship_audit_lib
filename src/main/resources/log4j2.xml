<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN">
    <Appenders>
        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout pattern="%d{yyyy-MM-dd HH:mm:ss.SSS} %level %msg%n"/>
        </Console>

        <RollingFile name="AuditFile" fileName="logs/audit.log"
                     filePattern="logs/audit-%i.log.gz">
            <PatternLayout pattern="%d{yyyy-MM-dd HH:mm:ss.SSS} %level %msg%n"/>
            <Policies>
                <SizeBasedTriggeringPolicy size="1MB"/>
            </Policies>
            <DefaultRolloverStrategy max="10"/>
        </RollingFile>

        <!-- Добавляем аппендер для HTTP запросов -->
        <RollingFile name="HttpRequestFile" fileName="logs/http-requests.log"
                     filePattern="logs/http-requests-%i.log.gz">
            <PatternLayout pattern="%d{yyyy-MM-dd HH:mm:ss.SSS} %level %msg%n"/>
            <Policies>
                <SizeBasedTriggeringPolicy size="1MB"/>
            </Policies>
            <DefaultRolloverStrategy max="10"/>
        </RollingFile>
    </Appenders>

    <Loggers>
        <Logger name="AUDIT_FILE" level="DEBUG" additivity="false">
            <AppenderRef ref="AuditFile"/>
        </Logger>

        <!-- Добавляем логгер для FileHttpRequestLogger -->
        <Logger name="HTTP_REQUEST_FILE" level="DEBUG" additivity="false">
            <AppenderRef ref="HttpRequestFile"/>
        </Logger>

        <Logger name="com.webbee.audit_lib.starter.logger.ConsoleAuditLogger" level="DEBUG" additivity="false">
            <AppenderRef ref="Console"/>
        </Logger>

        <Root level="INFO">
            <AppenderRef ref="Console"/>
        </Root>
    </Loggers>
</Configuration>